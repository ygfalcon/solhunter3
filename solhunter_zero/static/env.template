## Sample environment configuration for Solsniper
# Copy to `.env` and replace placeholders with real values.

# URL of the Solana RPC endpoint to use (required)
SOLANA_RPC_URL=https://mainnet.helius-rpc.com/?api-key=YOUR_HELIUS_KEY

# API key for the BirdEye price service (required)
BIRDEYE_API_KEY=b1e60d72780940d1bd929b9b2e9225e6

# Path to the JSON keypair file (optional)
# KEYPAIR_PATH=/home/user/.config/solana/id.json

# Jito block-engine credentials (optional, required for MEV bundles).
# New access tokens are not issued through the public flow; supply
# existing credentials tied to the same Solana keypair Solhunter uses
# for trading (for example via KEYPAIR_PATH) that Jito has whitelisted.
# The client signs the block engine challenge with that configured keypair
# to mint the JWT automatically when parameters are provided.
# JITO_RPC_URL=https://mainnet.block-engine.jito.wtf/api/v1/bundles
# JITO_AUTH=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.example
# JITO_WS_URL=wss://mainnet.block-engine.jito.wtf/api/v1/ws
# JITO_WS_AUTH=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.example
